server:
  port: 8080

# 日志配置
logging:
  level:
    root: INFO
    com.demo: DEBUG                      # 项目包 DEBUG 级别
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    path: ./logs

spring:
  application:
    name: demo
