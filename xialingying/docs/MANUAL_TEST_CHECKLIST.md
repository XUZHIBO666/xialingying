# Claw 助手人工测试清单

> 目的：验证自动测试无法覆盖的真实微信、真实模型、网络恢复和进程生命周期行为。  
> 执行原则：使用测试账号和测试群/会话；不要在截图、日志或记录中粘贴 API Key、Token、Cookie、AES Key、完整 contextToken。  
> 前置条件：使用 Maven 所采用的 JDK 21；不要执行 `clean`；保留 `target/voice-probe` 和 `target/surefire-reports`。

## 0. 测试记录信息

- [ ] 测试日期：
- [ ] Git 分支与提交：
- [ ] `git status --short` 已保存：
- [ ] `mvn --version` 已保存：
- [ ] 测试账号：
- [ ] 微信客户端版本：
- [ ] 网络环境：
- [ ] 测试执行人：

## 1. iLink 扫码登录

关联代码：

- `BotController.botPage()`
- `BotService.startLogin()`
- `BotService.startListening()`

步骤：

1. 运行 `DemoApplication.main()` 或 `mvn spring-boot:run`。
2. 访问 `http://localhost:8080/bot`。
3. 确认页面显示二维码。
4. 用测试账号扫码并确认。
5. 观察状态是否变为已登录，监听线程是否启动。

预期：

- [ ] 二维码可显示且不会泄露到非测试人员可访问位置。
- [ ] 扫码后状态变为已登录。
- [ ] 日志出现监听已启动，但测试记录中不复制凭据。
- [ ] 没有“暂不支持连接多个设备/OpenClaw”的阻断。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 2. 重启后是否重新扫码

关联代码：`BotService.credentials`、`BotService.startLogin()`。

步骤：

1. 完成登录后正常停止应用。
2. 重新启动应用。
3. 不访问 `/bot`，观察是否自动登录或自动申请二维码。
4. 再访问 `/bot`。

预期：

- [ ] 重启后旧内存凭据不再存在。
- [ ] 不访问 `/bot` 时不自动申请二维码。
- [ ] 访问 `/bot` 后要求重新扫码。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 3. 单条文本回复

关联代码：`BotService.processTextMessage()`、`AIService.chat()`、`BotService.sendReply()`。

步骤：

1. 测试用户发送一条唯一文本，例如“请只回复：文本链路成功”。
2. 记录接收时间、LLM 开始时间和微信回复时间。
3. 检查只收到一条回复。

预期：

- [ ] 日志只记录一次接收和一次发送。
- [ ] 回复内容符合请求。
- [ ] 没有泄露其他用户历史。
- [ ] 记录端到端耗时。

结果：`通过 / 失败 / 无法确认`  
耗时：  
证据位置：

## 4. 连续 20 条文本消息

关联代码：`BotService.createReplyExecutor()`、`submitReplyTask()`、`AIService.chat()`。

步骤：

1. 同一用户快速发送编号 01～20 的消息。
2. 每条要求回复对应编号。
3. 记录回复顺序、遗漏、重复和繁忙提示数量。

预期观察项：

- [ ] 20 条均被接收。
- [ ] 是否全部回复。
- [ ] 是否按 01～20 顺序回复。
- [ ] 是否出现历史错乱或编号混合。
- [ ] 是否触发容量 20 的队列上限。

当前源码不保证顺序；测试结果必须如实记录，不能因结果不符合预期而修改测试数据。

结果：`通过 / 失败 / 无法确认`  
乱序/遗漏/重复记录：

## 5. 两个不同用户同时对话

关联代码：`AIService.historyMap`、`AIService.chat()`。

步骤：

1. 用户 A 发送“记住我的代号是 A-731”。
2. 用户 B 同时发送“记住我的代号是 B-842”。
3. 两人分别追问“我的代号是什么？”。
4. 重复并发三轮。

预期：

- [ ] A 只得到 A-731。
- [ ] B 只得到 B-842。
- [ ] 没有跨用户历史泄漏。
- [ ] 记录同一时刻回复顺序和延迟。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 6. 相同 messageId 重复提交

关联代码：`BotService.startListening()`；当前没有 messageId 去重实现。

执行条件：需要可控的 SDK 测试代理、录制回放工具或供应商提供的重复投递方式。不要伪造无法确认的 SDK 字段。

步骤：

1. 捕获一条不含隐私的测试消息 DTO。
2. 使用相同 messageId 和 msgId 向消息处理入口提交两次。
3. 统计 LLM 调用和微信回复次数。

预期基线：

- [ ] 当前实现预计会处理两次；实际结果须记录。
- [ ] 如果 SDK 在业务入口前去重，标记“SDK行为，项目无法确认”。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 7. LLM 超时

关联代码：`AIService.chat()`。

步骤：

1. 在受控环境将文本 API 指向会延迟超过 60 秒的测试服务，不能使用生产服务制造故障。
2. 发送一条文本消息。
3. 观察线程、错误日志和用户回复。

预期：

- [ ] 请求在配置超时后结束，不无限挂起。
- [ ] 监听线程仍可继续接收其他消息。
- [ ] 用户是否收到“AI 暂时无响应”或无回复，按实际记录。
- [ ] 不在日志中输出凭据。

结果：`通过 / 失败 / 无法确认`  
实际耗时：

## 8. LLM 认证失败

关联代码：`AIService.chat()`、`BotController.initAutoReply()`。

步骤：

1. 使用专门的无效测试凭据或返回 401/403 的本地模拟服务。
2. 发送普通文本消息。
3. 检查错误分类、用户提示和后续消息处理。

预期：

- [ ] 进程不退出。
- [ ] 不记录 Authorization Header 或凭据。
- [ ] 当前实现可能统一返回“AI 暂时无响应”，记录实际表现。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 9. 图片接收和识别

关联代码：

- `BotService.processImageItem()`
- `BotService.processImageMessage()`
- `ImageRecognitionService.recognize()`

步骤：

1. 分别发送一张 JPEG、一张 PNG 和一张含文字图片。
2. 记录下载、预处理、识别和回复耗时。
3. 再发送不支持格式或超过限制的测试文件。

预期：

- [ ] 图片从微信 CDN 下载成功。
- [ ] JPEG/PNG 内容可识别。
- [ ] 图片中文字可读出。
- [ ] 失败时收到明确文字提示。
- [ ] 后续文本消息仍可正常处理。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 10. 图片生成和发送

关联代码：

- `ImageGenerationService.isImageRequest()`
- `ImageGenerationService.generateImage()`
- `BotService.sendImageReply()`

步骤：

1. 发送 `/draw 一只戴蓝色帽子的白猫`。
2. 发送中文触发词版本。
3. 记录平台请求、下载、上传微信和显示耗时。
4. 验证微信中图片可打开且不是文字链接。

预期：

- [ ] 触发词被正确识别。
- [ ] 使用配置的图片生成模型。
- [ ] 只发送一条图片消息。
- [ ] 图片在微信中可正常查看。
- [ ] 平台失败时收到文字提示。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 11. 语音下载

关联代码：`BotService.processVoiceProbe()`。

步骤：

1. 清楚地朗读三条不含隐私、内容已知的短语音。
2. 检查 `target/voice-probe` 是否新增 `.bin` 和 `.transcript.txt`。
3. 对照 SDK 元数据和文件头。

预期：

- [ ] 每条语音对应一个非空 `.bin`。
- [ ] 文件头符合已探测的微信 SILK V3 格式，或如实记录新格式。
- [ ] 官方转写文本被保存但不写入普通日志。
- [ ] 记录 playtime、sampleRate、bitsPerSample、encodeType。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 12. SILK 转 PCM

关联文档：`docs/voice-feature-investigation.md`。

步骤：

1. 对第 11 项的新 `.bin` 使用已校验的专用 SILK decoder。
2. 确认是否需要移除微信前导字节。
3. 检查退出码和输出文件大小。
4. 使用音频工具确认 PCM 参数。

预期：

- [ ] 解码进程在限定时间内退出。
- [ ] 输出为 16 kHz、单声道、16-bit little-endian PCM，或记录实际差异。
- [ ] 解码时长与 `playtime` 大致一致。

结果：`通过 / 失败 / 无法确认`  
命令（不得包含凭据）：  
证据位置：

## 13. PCM 转 WAV

关联文档：`docs/voice-feature-investigation.md`。

步骤：

1. 使用 FFmpeg 将第 12 项 PCM 封装为 WAV。
2. 使用 ffprobe 检查格式、采样率、声道和时长。
3. 在本机播放确认没有明显变速或静音。

预期：

- [ ] WAV 为 PCM signed 16-bit little-endian。
- [ ] 采样率 16000 Hz。
- [ ] 单声道。
- [ ] 可正常播放。

结果：`通过 / 失败 / 无法确认`  
ffprobe 摘要：  
证据位置：

## 14. 天气查询

关联代码：

- `BotController.initAutoReply()`
- `BotController.extractCity()`
- `WeatherUtil.getWeather()`
- `WeatherController.getWeather()`

步骤：

1. 微信发送“杭州天气怎么样？”。
2. 微信发送英文城市天气问题。
3. 调用 REST `GET /api/weather?city=杭州`。
4. 测试不存在城市和非法字符。

预期：

- [ ] 微信关键词正确进入天气分支。
- [ ] 城市提取正确。
- [ ] 回复包含实时温度、天气、湿度或风力。
- [ ] REST 返回统一 Response 结构。
- [ ] 无效城市收到明确错误。

结果：`通过 / 失败 / 无法确认`  
证据位置：

## 15. 断网后恢复

关联代码：`BotService.startListening()`。

步骤：

1. 登录并确认文本消息可正常回复。
2. 断开测试主机网络 30～60 秒。
3. 恢复网络。
4. 再发送一条文本消息。
5. 观察是否需要重新扫码。

预期观察项：

- [ ] 断网期间进程不退出。
- [ ] 日志约每 5 秒记录一次监听异常，而不是高频死循环。
- [ ] 恢复后能否自动收到消息。
- [ ] 如果凭据失效，是否持续失败。
- [ ] 是否需要手工重新扫码。

结果：`通过 / 失败 / 无法确认`  
恢复耗时：  
证据位置：

## 16. 程序正常关闭

关联代码：`BotService.shutdownReplyExecutor()`。

步骤：

1. 登录并让监听线程处于运行状态。
2. 保持一个慢业务请求执行。
3. 从 IDEA 正常停止或向进程发送正常终止信号。
4. 检查进程、线程、端口和文件句柄是否释放。

预期：

- [ ] Spring 开始关闭流程。
- [ ] 回复线程池停止。
- [ ] 登录和监听线程不再运行。
- [ ] 8080 端口释放。
- [ ] 没有新的循环重试日志。
- [ ] 进程在合理时间内退出。

当前源码只明确关闭回复线程池，其余项需重点记录。

结果：`通过 / 失败 / 无法确认`  
关闭耗时：  
证据位置：

## 17. 测试结束检查

- [ ] 再次运行 `git status --short`，确认人工测试未意外修改源码或配置。
- [ ] 不提交日志、二维码、contextToken、语音隐私内容或任何凭据。
- [ ] 保存脱敏后的测试时间、结果和错误码。
- [ ] 对“无法确认”的项目说明缺少的权限、工具或测试条件。
- [ ] 不执行 `clean`，保留语音和 Surefire 诊断产物。

